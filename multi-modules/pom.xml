<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.touin.thierry.sb.test</groupId>
  <artifactId>springboot-tester</artifactId>
  <version>0.0.1-SNAPSHOT</version>
  <packaging>pom</packaging>
  <name>Projet Spring Boot Multi-Modules</name>

  <modules>
    <module>springboot-tester-domain</module>
    <module>springboot-tester-application</module>
    <module>springboot-tester-infrastructure</module>
    <module>springboot-tester-infrastructure-mock</module>
    <module>springboot-tester-exposition</module>
  </modules>

  <properties>
    <java.version>17</java.version>
    <spring.boot.version>3.5.0</spring.boot.version>
    <slf4j.version>2.0.13</slf4j.version>
    <!-- <maven-surefire-plugin.version>3.5.2</maven-surefire-plugin.version>
    <io.cucumber.version>7.15.0</io.cucumber.version> -->
    <maven-surefire-plugin.version>2.12.4</maven-surefire-plugin.version> 
    <io.cucumber.version>7.14.0</io.cucumber.version>
    <jacoco.version>0.8.12</jacoco.version>
  </properties>


  <profiles>
    <profile>
      <id>default</id>
      <activation>
        <activeByDefault>true</activeByDefault>
      </activation>      
      <build>
        <plugins>
          <plugin>
            <artifactId>maven-surefire-plugin</artifactId>
            <groupId>org.apache.maven.plugins</groupId>
            <version>${maven-surefire-plugin.version}</version>
            <configuration>
              <includes>
                <include>**/CucumberDefaultTest.java</include>
                <!-- <include>com.touin.thierry.sb.test.springboottester.CucumberDefaultTest.java</include> -->
              </includes>
              <!-- <failIfNoTests>true</failIfNoTests> -->
            </configuration>
          </plugin>
        </plugins>
      </build>      
    </profile>  
    <profile>
      <id>mock</id>
      <modules>
        <module>springboot-tester-infrastructure-mock</module>
      </modules>
      <build>
        <plugins>
          <plugin>
            <artifactId>maven-surefire-plugin</artifactId>
            <groupId>org.apache.maven.plugins</groupId>
            <version>${maven-surefire-plugin.version}</version>
            <configuration>
              <includes>
                <include>**/CucumberMockTest.java</include>
              </includes>
              <!-- <failIfNoTests>true</failIfNoTests> -->
            </configuration>
          </plugin>
        </plugins>
      </build>      
    </profile>
  </profiles>

  <dependencyManagement>
    <dependencies>
      <!-- Définis ici les versions communes -->
      <dependency>
          <groupId>org.springframework.boot</groupId>
          <artifactId>spring-boot-starter</artifactId>
          <version>${spring.boot.version}</version>
      </dependency>
      <dependency>
          <groupId>org.springframework.boot</groupId>
          <artifactId>spring-boot-devtools</artifactId>
          <version>${spring.boot.version}</version>
          <scope>runtime</scope>
          <optional>true</optional>
      </dependency>    

      <!-- Test -->
      <dependency>
        <groupId>io.cucumber</groupId>
        <artifactId>cucumber-java</artifactId>
        <version>${io.cucumber.version}</version>
        <scope>test</scope>
      </dependency>
      <dependency>
        <groupId>io.cucumber</groupId>
        <artifactId>cucumber-spring</artifactId>
        <version>${io.cucumber.version}</version>
        <scope>test</scope>
      </dependency>
      <dependency>
        <groupId>io.cucumber</groupId>
        <artifactId>cucumber-junit</artifactId>
        <version>${io.cucumber.version}</version>
        <scope>test</scope>
      </dependency>
      <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-test</artifactId>
        <version>${spring.boot.version}</version>
        <scope>test</scope>
      </dependency>

      <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-dependencies</artifactId>
        <version>${spring.boot.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>


    </dependencies>
  </dependencyManagement>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.springframework.boot</groupId>
          <artifactId>spring-boot-maven-plugin</artifactId>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>3.11.0</version>
          <configuration>
            <source>${java.version}</source>
            <target>${java.version}</target>
            <release>${java.version}</release>
          </configuration>
        </plugin>
        








      <!-- JaCoCo : prepare-agent (pour générer jacoco.exec dans chaque module)
           et report-aggregate (exécuté dans le parent packaging=pom) -->
      <plugin>
        <groupId>org.jacoco</groupId>
        <artifactId>jacoco-maven-plugin</artifactId>
        <version>${jacoco.version}</version>
        <executions>
          <execution>
            <id>prepare-agent</id>
            <goals>
              <goal>prepare-agent</goal>
            </goals>
            <!-- pas de <phase> pour le préparer avant surefire/failsafe -->
          </execution>

          <!-- report-aggregate : exécuté dans le parent (packaging pom) -->
          <execution>
            <id>report-aggregate</id>
            <goals>
              <goal>report-aggregate</goal>
            </goals>
            <phase>verify</phase>
            <configuration>
              <outputDirectory>${project.reporting.outputDirectory}/jacoco-aggregate</outputDirectory>
              <title>${project.name} - Coverage (aggregate)</title>
            </configuration>
          </execution>

        </executions>
      </plugin>

      <!-- Surefire : veiller à injecter l'argLine de Jacoco -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <!-- <version>${maven-surefire-plugin.version}</version> -->
        <version>3.1.2</version> <!-- version moderne compatible Java17 -->
        <configuration>
          <!-- concatène si tu as déjà un argLine dans un module -->
          <argLine>${jacoco.argLine} ${argLine}</argLine>
    <!-- comportement du fork -->
    <forkCount>1</forkCount>
    <reuseForks>true</reuseForks>

    <!-- garder des logs utiles -->
    <trimStackTrace>false</trimStackTrace>

    <!-- options JVM pour éviter OOM (ajuste selon ta machine) -->
    <forkedProcessTimeoutInSeconds>600</forkedProcessTimeoutInSeconds>
    <additionalClasspathElements/>
    <environmentVariables/>
    <testFailureIgnore>false</testFailureIgnore>

        </configuration>
      </plugin>

      <!-- Failsafe : si tu utilises Failsafe pour des tests d'intégration -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-failsafe-plugin</artifactId>
        <version>3.1.2</version>
        <configuration>
          <argLine>${jacoco.argLine} ${argLine}</argLine>
    <forkCount>1</forkCount>
    <reuseForks>true</reuseForks>
    <trimStackTrace>false</trimStackTrace>
        </configuration>
      </plugin>

    </plugins>
  </pluginManagement>

  <!-- Appliquer les plugins (assure-toi qu'ils sont activés dans la build) -->
  <plugins>
    <!-- applique surefire (configuration effective issue de pluginManagement) -->
    <plugin>
      <groupId>org.apache.maven.plugins</groupId>
      <artifactId>maven-surefire-plugin</artifactId>
    </plugin>

    <!-- applique failsafe si besoin -->
    <plugin>
      <groupId>org.apache.maven.plugins</groupId>
      <artifactId>maven-failsafe-plugin</artifactId>
    </plugin>

    <!-- applique jacoco prepare-agent au niveau parent (héritage) -->
    <plugin>
      <groupId>org.jacoco</groupId>
      <artifactId>jacoco-maven-plugin</artifactId>
    </plugin>
  </plugins>
</build>





</project>
